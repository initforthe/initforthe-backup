<%- if @gzip_rsyncable or @gzip_compression_level -%>
  compress_with Gzip do |compression|
    compression.level     = <%= @gzip_compression_level %>
    compression.rsyncable = <%= @gzip_rsyncable %>
  end
<%- else -%>
  compress_with Gzip
<%- end -%>
