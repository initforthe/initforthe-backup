  store_with Ninefold do |nf|
    nf.storage_token   = '<%= @ninefold_storage_token %>'
    nf.storage_secret  = '<%= @ninefold_storage_secret %>'
    nf.path            = '<%= @ninefold_path %>'
    nf.keep            = <%= @ninefold_keep %>
  end
